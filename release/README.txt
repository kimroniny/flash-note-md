闪记 —— 开会用的 Markdown 笔记（所写即所见）

1. 双击 FlashNote-Setup.exe 覆盖安装（不用管理员）
2. 默认把笔记存在「文档\闪记」，可在应用里点「存储」改目录
3. 顶栏「设置」可改存储目录、主题、字体、字号；列表里 Tab 缩进

便携版：FlashNote.exe 直接双击。
卸载：Windows 设置 → 应用 → 闪记

写法：# 标题  **强调**  `代码`  - 列表  - [ ] 待办
Ctrl+N 新建会议，Ctrl+; 插入时间，Ctrl+S 保存。
